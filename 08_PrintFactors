#include <stdio.h>



int main() {
    int n;

    // Take input
    scanf("%d", &n);

    // Print all factors
    for (int i = 1; i <= n; i++) {
        if (n % i == 0) {
            printf("%d ", i);
        }
    }

    return 0;
}